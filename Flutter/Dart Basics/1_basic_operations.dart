// Dart!!!


// Setup:- Dartpad online editor (https://dartpad.dev/)

// Code:-
void main(){
  //This is comment section
  print("Hello world"); //comments
  print("This is first program");
  print(2+3); //add
  print(12/4); //divide
  print(true); //boolean
}

// Datatypes:- Int, double || Strings || Booleans || Lists(also called arrays) || Maps || Runes(for unicode characters in string) || Symbols

// Default values are NULL as they are objects

// Var datatype is inferred as integer or string or boolean automatically
