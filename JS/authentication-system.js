// Build a simple authentication system. Users can access a secret page only if they meet the following criteria:

// 1. They must be logged in.
// 2. They must be at least 18 years old.
// 3. Their account must not be suspended.

// let firstName;
// let lastName;
// let mail_id;
// let age;

// if (firstName === "Aditya" && mail_id === "aditya.zanzane@coditas.com") {
//     if (age > 18) {
//         console.log("Your account is not suspended.");
//     } else {
//         console.log("You must be at least 18 years old to access the secret page.");
//     }
// } else {
//     console.log("Please log in with valid credentials.");
// }

// login() ? (age>18) ? isSuspended ? console.log("Login") : console.log("failed login") : console.log("Minor") : console.log("Not Login");
